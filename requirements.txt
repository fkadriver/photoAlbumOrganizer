# Image processing
Pillow>=10.0.0
pillow-heif>=0.13.0
imagehash>=4.3.1
opencv-python>=4.8.0

# Face detection (install ONE of the following groups):
#
# Option 1: MediaPipe (default) - no compilation required, no system deps
mediapipe>=0.10.0
#
# Option 2: face_recognition (dlib-based) - adds face encoding/identity matching
# Requires C++ build tools and cmake. On NixOS use 'dlib-binary' to skip compilation:
#   pip install dlib-binary face_recognition face_recognition_models
#   (also needs: setuptools<71)
# face_recognition>=1.3.0
# face_recognition_models>=0.3.0
# dlib>=19.24.0
# setuptools<71  # face_recognition_models requires pkg_resources

# Advanced face processing (optional - for face swapping feature)
# face-alignment>=1.3.0

# Utilities
numpy>=1.24.0
scipy>=1.10.0

# Apple Photos integration (macOS only)
osxphotos>=0.68.0

# Immich integration
requests>=2.31.0

# Use strict_env to prevent unnecessary reloads
strict_env

# Use flake with layout to stabilize environment
use flake

# Only reload when these files change
watch_file flake.nix
watch_file flake.lock
watch_file requirements.txt

# Use the pure NixOS flake (recommended - no venv issues)
use flake .#default

# Alternative: Use traditional shell.nix
# use nix

# Alternative: Use pure shell.nix (rename shell-pure.nix to shell.nix first)
# use nix
